# 🚀 Cisco IOS CLI - Các Chế Độ Làm Việc (Modes) & Câu Lệnh Cơ Bản

Tài liệu tóm tắt kiến thức về cấu trúc hệ điều hành Cisco IOS, các chế độ làm việc (Modes) và các câu lệnh thao tác cơ bản trên thiết bị Router/Switch của Cisco.

---

## 📌 1. Các chế độ làm việc (Modes) trên Cisco IOS

Tương tự như giao diện bảo mật trên điện thoại (Màn hình khóa ➔ Màn hình chính ➔ Cài đặt hệ thống ➔ Cài đặt ứng dụng), Cisco IOS phân chia thành 4 chế độ làm việc chính từ ngoài vào trong:

### 1.1. User EXEC Mode (`Router>`)
* **Dấu hiệu nhận biết:** `Tên_thiết_bị>`
* **Chức năng:** Chế độ xem cơ bản. Chỉ cho phép kiểm tra một số thông số cơ bản và kết nối mạng đơn giản (`ping`, `traceroute`). Không xem được thông số cấu hình sâu hay mật khẩu và **không thể cấu hình/thay đổi thông số**.

### 1.2. Privileged EXEC Mode (`Router#`)
* **Dấu hiệu nhận biết:** `Tên_thiết_bị#`
* **Chức năng:** Chế độ xem toàn quyền (Read-only). Cho phép xem/hiển thị (`show`) toàn bộ thông số của thiết bị (mật khẩu, file cấu hình chi tiết, bảng tuyến...), nhưng vẫn **chưa thể trực tiếp sửa đổi cấu hình**.

### 1.3. Global Configuration Mode (`Router(config)#`)
* **Dấu hiệu nhận biết:** `Tên_thiết_bị(config)#`
* **Chức năng:** Chế độ cấu hình toàn cục. Cho phép thay đổi các thông số ảnh hưởng đến toàn bộ thiết bị (ví dụ: đổi tên thiết bị, cài đặt mật khẩu truy cập, cấu hình định tuyến toàn cục...).

### 1.4. Sub-Configuration Modes (`Router(config-xxx)#`)
* **Dấu hiệu nhận biết:** `Router(config-if)#`, `Router(config-line)#`, `Router(config-router)#`...
* **Chức năng:** Chế độ cấu hình thành phần. Cho phép cấu hình chi tiết cho một nhánh/thành phần nhỏ cụ thể (ví dụ: gán IP cho cổng Interface, cấu hình cổng Console, v.v.).

---

## 🔄 2. Sơ đồ chuyển đổi giữa các Mode

```text
       [ User EXEC Mode ]            (Router>)
               │
               │  gõ: enable
               ▼
    [ Privileged EXEC Mode ]         (Router#)
               │
               │  gõ: configure terminal
               ▼
  [ Global Configuration Mode ]      (Router(config)#)
               │
               ├────────────────────────────────────────┐
               │ gõ: interface <tên_cổng>              │ gõ: line console 0
               ▼                                        ▼
    [ Interface Config Mode ]                [ Line Config Mode ]
    (Router(config-if)#)                     (Router(config-line)#)
```

---

## 🛠️ 3. Bảng tổng hợp câu lệnh thao tác cơ bản

| Thao tác | Câu lệnh | Từ Mode | Đến Mode / Kết quả |
| :--- | :--- | :--- | :--- |
| **Vào chế độ Privileged** | `enable` (viết tắt: `en`) | `Router>` | `Router#` |
| **Vào chế độ Global Config** | `configure terminal` (viết tắt: `conf t`) | `Router#` | `Router(config)#` |
| **Vào chế độ Interface** | `interface <tên_cổng>` *(VD: `interface g0/0`)* | `Router(config)#` | `Router(config-if)#` |
| **Đổi tên thiết bị** | `hostname <tên_mới>` *(VD: `hostname R1`)* | `Router(config)#` | Đổi prompt thành `R1(config)#` |
| **Lùi về 1 cấp Mode** | `exit` | Bất kỳ Mode nào | Mode cấp trước đó |
| **Thoát thẳng về Privileged** | `end` hoặc nhấn `Ctrl + Z` / `Ctrl + C` | Mọi Sub-Config Mode | `Router#` |
| **Lưu cấu hình vào NVRAM** | `copy running-config startup-config`<br>*(hoặc lệnh `write memory` / `wr`)* | `Router#` | Đảm bảo cấu hình không mất khi mất điện/reboot |

---

## 💡 4. Mẹo & Phím tắt hữu ích trong Cisco IOS CLI

> [!TIP]
> * **Phím `Tab`:** Tự động hoàn thành câu lệnh (Ví dụ: gõ `conf` rồi nhấn `Tab` $\rightarrow$ tự điền `configure`).
> * **Phím `?`:** Tra cứu danh sách câu lệnh khả dụng tiếp theo.
> * **Phím mũi tên Lên/Xuống:** Xem lại lịch sử các câu lệnh vừa gõ.
> * **Hủy câu lệnh đang gõ / Ngắt lệnh:** Nhấn `Ctrl + C` hoặc `Ctrl + Shift + 6` (để ngắt ping/domain lookup).
