
# 🚀 Cisco IOS CLI - Chế Độ Làm Việc, Lệnh Cơ Bản & Bảo Mật


## 📌 1. Các chế độ làm việc (Modes) trên Cisco IOS

Tương tự như giao diện bảo mật trên điện thoại (Màn hình khóa ➔ Màn hình chính ➔ Cài đặt hệ thống ➔ Cài đặt ứng dụng), Cisco IOS phân chia thành 4 chế độ làm việc chính:

### 1.1. User EXEC Mode (`Router>`)
* **Dấu hiệu:** `Tên_thiết_bị>`
* **Chức năng:** Chế độ xem cơ bản. Chỉ cho phép kiểm tra một số thông số cơ bản và kết nối mạng đơn giản (`ping`, `traceroute`). Không thể cấu hình/thay đổi thông số.

### 1.2. Privileged EXEC Mode (`Router#`)
* **Dấu hiệu:** `Tên_thiết_bị#`
* **Chức năng:** Xem toàn bộ thông số chi tiết (`show running-config`, mật khẩu, bảng tuyến...), nhưng vẫn **chưa thể trực tiếp sửa đổi cấu hình**.

### 1.3. Global Configuration Mode (`Router(config)#`)
* **Dấu hiệu:** `Tên_thiết_bị(config)#`
* **Chức năng:** Cấu hình toàn cục. Thay đổi các thông số ảnh hưởng toàn thiết bị (đổi tên `hostname`, đặt mật khẩu, mã hóa, banner motd...).

### 1.4. Sub-Configuration Modes (`Router(config-xxx)#`)
* **Dấu hiệu:** `Router(config-if)#`, `Router(config-line)#`, `Router(config-router)#`...
* **Chức năng:** Cấu hình chi tiết cho từng thành phần/nhánh nhỏ (cổng Interface, cổng Line Console, v.v.).

---

## 🔄 2. Sơ đồ chuyển đổi giữa các Mode

```text
       [ User EXEC Mode ]            (Router>)
               │
               │  gõ: enable
               ▼
    [ Privileged EXEC Mode ]         (Router#)
               │
               │  gõ: configure terminal
               ▼
  [ Global Configuration Mode ]      (Router(config)#)
               │
               ├────────────────────────────────────────┐
               │ gõ: interface <tên_cổng>              │ gõ: line console 0
               ▼                                        ▼
    [ Interface Config Mode ]                [ Line Config Mode ]
    (Router(config-if)#)                     (Router(config-line)#)
```

---

## 🛠️ 3. Các câu lệnh thao tác & Chuyển Mode cơ bản

| Thao tác | Câu lệnh | Từ Mode | Đến Mode / Kết quả |
| :--- | :--- | :--- | :--- |
| **Vào Privileged Mode** | `enable` (viết tắt: `en`) | `Router>` | `Router#` |
| **Vào Global Config Mode** | `configure terminal` (viết tắt: `conf t`) | `Router#` | `Router(config)#` |
| **Đổi tên thiết bị** | `hostname <tên_mới>` | `Router(config)#` | Prompt đổi tên (VD: `R1(config)#`) |
| **Vào cấu hình Cổng** | `interface <tên_cổng>` *(VD: `interface g0/0`)* | `Router(config)#` | `Router(config-if)#` |
| **Vào cấu hình Console** | `line console 0` | `Router(config)#` | `Router(config-line)#` |
| **Lùi 1 cấp Mode** | `exit` | Bất kỳ Mode nào | Mode cấp trước đó |
| **Thoát thẳng về Privileged**| `end` hoặc tổ hợp `Ctrl + Z` / `Ctrl + C` | Mọi Sub-Config Mode | `Router#` |
| **Lưu cấu hình vào NVRAM** | `copy running-config startup-config` *(viết tắt: `wr`)* | `Router#` | Đảm bảo cấu hình không mất khi khởi động lại |

---

## 🔐 4. Đặt mật khẩu & Mã hóa mật khẩu

### 4.1. Mật khẩu bảo vệ Privileged EXEC Mode (`enable password` vs `enable secret`)
Để ngăn chặn truy cập trái phép từ User Mode vào Privileged Mode:

* **Enable Password (Không an toàn):**
  ```text
  Router(config)# enable password <mật_khẩu>
  ```
  *Lưu ý:* Mật khẩu được lưu dưới dạng văn bản thuần (plaintext). Nếu gõ `show running-config` sẽ nhìn thấy rõ mật khẩu.

* **Enable Secret (Khuyên dùng - An toàn cao):**
  ```text
  Router(config)# enable secret <mật_khẩu>
  ```
  *Lưu ý:* Mật khẩu được mã hóa băm (Hash) an toàn. 

> [!IMPORTANT]
> **Độ ưu tiên:** Khi cấu hình cả hai câu lệnh trên cùng thiết bị, Cisco IOS sẽ **luôn ưu tiên dùng `enable secret`** để đăng nhập.

---

### 4.2. Mật khẩu truy cập cổng Console (`Line Console 0`)
Bảo vệ thiết bị ngay khi ai đó cắm cáp Console trực tiếp vào Router/Switch:

```text
Router(config)# line console 0
Router(config-line)# password <mật_khẩu>
Router(config-line)# login
Router(config-line)# exit
```
*(Bắt buộc phải có lệnh `login` thì thiết bị mới hỏi mật khẩu khi kết nối Console).*

---

### 4.3. Mã hóa toàn bộ mật khẩu dạng thô (`service password-encryption`)
Tự động mã hóa tất cả mật khẩu đang ở dạng chưa mã hóa (như `enable password` hay mật khẩu `line console`) trong file cấu hình:

```text
Router(config)# service password-encryption
```

---

## 📢 5. Cấu hình Banner cảnh báo (`Banner MOTD`)

**Khái niệm:** MOTD (Message of the Day) là thông điệp/cảnh báo hiển thị trên màn hình ngay khi người dùng vừa kết nối vào thiết bị (trước khi vào User Mode).

**Cú pháp:**
```text
Router(config)# banner motd <ký_tự_bắt_đầu_và_kết_thúc> <nội_dung_thông_điệp> <ký_tự_bắt_đầu_và_kết_thúc>
```

**Ví dụ:**
```text
Router(config)# banner motd #
------------------------------------------------
 CANH BAO: HE THONG QUAN TRONG!
 CHI DANG NHAP KHI DUOC PHEP.
------------------------------------------------
#
```

> [!NOTE]
> * Ký tự phân cách (như `#`, `$`, `"`) được dùng để mở đầu và kết thúc chuỗi thông điệp. Ký tự kết thúc phải giống hệt ký tự mở đầu.
> * Bạn có thể gõ nội dung Banner trên nhiều dòng bằng cách nhấn `Enter` thoải mái, cho đến khi gõ lại ký tự phân cách đó và nhấn `Enter` để hoàn tất.

---

## 💡 6. Mẹo & Phím tắt hữu ích

> [!TIP]
> * **Phím `Tab`:** Tự động điền đầy đủ câu lệnh (VD: gõ `conf` + `Tab` $\rightarrow$ `configure`).
> * **Dấu `?`:** Liệt kê các lệnh có thể sử dụng tiếp theo.
> * **`Ctrl + C` hoặc `Ctrl + Shift + 6`:** Ngắt lệnh đang chạy (như `ping` hoặc tra cứu domain).
