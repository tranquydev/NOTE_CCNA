# 🚀 Cisco IOS CLI & Networking Fundamentals - Cheat Sheet & Guide


## 📌 1. Các chế độ làm việc (Modes) trên Cisco IOS

Hệ điều hành Cisco IOS phân chia thành 4 chế độ làm việc chính từ ngoài vào trong:

### 1.1. User EXEC Mode (`Router>`)
* **Dấu hiệu:** `Tên_thiết_bị>`
* **Chức năng:** Chế độ xem cơ bản. Chỉ cho phép kiểm tra một số thông số cơ bản và kết nối mạng đơn giản (`ping`, `traceroute`). Không thể cấu hình/thay đổi thông số.

### 1.2. Privileged EXEC Mode (`Router#`)
* **Dấu hiệu:** `Tên_thiết_bị#`
* **Chức năng:** Xem toàn bộ thông số chi tiết (`show running-config`, mật khẩu, bảng tuyến...), nhưng vẫn **chưa thể trực tiếp sửa đổi cấu hình**.

### 1.3. Global Configuration Mode (`Router(config)#`)
* **Dấu hiệu:** `Tên_thiết_bị(config)#`
* **Chức năng:** Cấu hình toàn cục. Thay đổi các thông số ảnh hưởng toàn thiết bị (đổi tên `hostname`, đặt mật khẩu, mã hóa, banner motd, default-gateway...).

### 1.4. Sub-Configuration Modes (`Router(config-xxx)#`)
* **Dấu hiệu:** `Router(config-if)#`, `Router(config-line)#`, `Router(config-router)#`...
* **Chức năng:** Cấu hình chi tiết cho từng thành phần/nhánh nhỏ (cổng Interface vật lý, VLAN ảo, cổng Line Console, v.v.).

---

## 🔄 2. Sơ đồ chuyển đổi giữa các Mode

```text
       [ User EXEC Mode ]            (Router>)
               │
               │  gõ: enable
               ▼
    [ Privileged EXEC Mode ]         (Router#)
               │
               │  gõ: configure terminal
               ▼
  [ Global Configuration Mode ]      (Router(config)#)
               │
               ├────────────────────────────────────────┐
               │ gõ: interface <tên_cổng>              │ gõ: line console 0
               ▼                                        ▼
    [ Interface Config Mode ]                [ Line Config Mode ]
    (Router(config-if)#)                     (Router(config-line)#)
```

---

## 🛠️ 3. Các câu lệnh thao tác & Chuyển Mode cơ bản

| Thao tác | Câu lệnh | Từ Mode | Đến Mode / Kết quả |
| :--- | :--- | :--- | :--- |
| **Vào Privileged Mode** | `enable` *(viết tắt: `en`)* | `Router>` | `Router#` |
| **Vào Global Config Mode** | `configure terminal` *(viết tắt: `conf t`)* | `Router#` | `Router(config)#` |
| **Đổi tên thiết bị** | `hostname <tên_mới>` | `Router(config)#` | Prompt đổi tên (VD: `R1(config)#`) |
| **Vào cấu hình Cổng** | `interface <tên_cổng>` *(VD: `interface g0/0`)* | `Router(config)#` | `Router(config-if)#` |
| **Vào cấu hình Console** | `line console 0` | `Router(config)#` | `Router(config-line)#` |
| **Lùi 1 cấp Mode** | `exit` | Bất kỳ Mode nào | Mode cấp trước đó |
| **Thoát thẳng về Privileged**| `end` hoặc tổ hợp `Ctrl + Z` / `Ctrl + C` | Mọi Sub-Config Mode | `Router#` |

---

## 🌐 4. Cú Pháp Cấu Hình IP Trên Cisco IOS

### 4.1. Cấu hình IP trên Router (Physical Interface)

```text
Router# configure terminal
Router(config)# interface gigabitEthernet 0/0/0
Router(config-if)# ip address 172.31.103.1 255.255.255.224
Router(config-if)# no shutdown
Router(config-if)# exit
```

> [!IMPORTANT]
> **Lưu ý:** Mặc định cổng Router ở trạng thái `Shutdown` (Tắt - Đèn đỏ). Bắt buộc phải có lệnh `no shutdown` (viết tắt `no sh`) để kích hoạt cổng sáng đèn màu xanh.

---

### 4.2. Cấu hình IP Quản lý & Default Gateway trên Switch (Layer 2)

```text
Switch# configure terminal
Switch(config)# interface vlan 1
Switch(config-if)# ip address 172.31.103.2 255.255.255.224
Switch(config-if)# no shutdown
Switch(config-if)# exit
Switch(config)# ip default-gateway 172.31.103.1
```

---

## 🧮 5. Subnet Mask, Wildcard Mask & Quy Hoạch Địa Chỉ Mạng (VLSM)

### 5.1. Khái niệm Subnet Mask & Wildcard Mask

* **Subnet Mask (Mặt nạ mạng):**
  * **Bản chất:** Gồm 32 bit (4 Octet), giúp thiết bị xác định phần **Network ID** (Mạng) và phần **Host ID** (Thiết bị).
  * **Cách tính nhanh:** Subnet dạng `/N` $\rightarrow$ Cho $N$ bit đầu tiên bằng `1`, các bit còn lại bằng `0`.
    * Ví dụ `/24` $\rightarrow$ `255.255.255.0`
    * Ví dụ `/27` $\rightarrow$ `255.255.255.224`
    * Ví dụ `/28` $\rightarrow$ `255.255.255.240`

* **Wildcard Mask:**
  * **Bản chất:** Dùng trong cấu hình Access Control List (ACL), OSPF...
  * **Công thức tính nhanh:** `Wildcard Mask = 255.255.255.255 - Subnet Mask`
  * **Ví dụ:** Subnet Mask `/26` (`255.255.255.192`) $\rightarrow$ `Wildcard Mask = 0.0.0.63`.

---

### 5.2. Thực hành chia địa chỉ IP VLSM (Bài Lab 11.9.3 Case Study)

**Đề bài:** Cho dải IP gốc `172.31.103.0/24`. Chia mạng cho 4 phòng LAN và 1 đường WAN nối 2 Router.

#### Bảng quy tắc gán IP chuẩn theo đề bài:

| Mạng | Subnet Address | First Usable IP<br>*(Gán cho Router)* | Second Usable IP<br>*(Gán cho Switch VLAN 1)* | Last Usable IP<br>*(Gán cho PC)* | Broadcast Address |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Room 114 (/27)** | `172.31.103.0` | `172.31.103.1` | `172.31.103.2` | `172.31.103.30` | `172.31.103.31` |
| **Room 279 (/27)** | `172.31.103.32` | `172.31.103.33` | `172.31.103.34` | `172.31.103.62` | `172.31.103.63` |
| **Room 312 (/28)** | `172.31.103.64` | `172.31.103.65` | `172.31.103.66` | `172.31.103.78` | `172.31.103.79` |
| **Room 407 (/28)** | `172.31.103.80` | `172.31.103.81` | `172.31.103.82` | `172.31.103.94` | `172.31.103.95` |
| **Link WAN (/30)** | `172.31.103.96` | `172.31.103.97` *(R1)* | `172.31.103.98` *(R2)* | N/A | `172.31.103.99` |

---

## 🌍 6. Luồng Hoạt Động Dịch Vụ Ứng Dụng Mạng (Web, DNS, Email)

### 6.1. Dịch vụ phân giải tên miền (DNS Server)
* **Vai trò:** Ánh xạ từ tên miền dễ nhớ (Domain) sang địa chỉ IP thực tế của máy chủ (bằng bản ghi **A Record**).
* **Ứng dụng:** Mọi thiết bị Host bắt buộc phải gán DNS Server IP. 

> [!NOTE]
> **Mẹo chẩn đoán sự cố:** Nếu máy tính **ping thành công** tới IP ngoài (như `8.8.8.8`) nhưng **không thể mở trang web** (VD: google.com), **90% nguyên nhân** là do chưa gán hoặc hỏng cấu hình DNS Server.

---

### 6.2. Dịch vụ Email (SMTP, POP3 / IMAP)
* **Cơ chế gửi/nhận:** Quá trình truyền Email giữa 2 người dùng **không bao giờ truyền trực tiếp** giữa 2 PC, mà bắt buộc qua **Mail Server trung gian** (như Gmail Server).
* **Luồng xử lý:**
  1. `PC_Sender` dùng DNS phân giải IP của Mail Server (`gmail.com`).
  2. `PC_Sender` đẩy thư lên Mail Server (qua giao thức SMTP).
  3. Mail Server lưu thư vào Hộp thư đến (Inbox) của `PC_Receiver`.
  4. `PC_Receiver` chủ động kết nối lên Mail Server để tải/đọc thư (qua POP3 hoặc IMAP).

> [!WARNING]
> Thông báo "Gửi Mail thành công" chỉ có nghĩa là thư đã đẩy thành công lên **Mail Server**. Người nhận có đọc được hay không tùy thuộc vào kết nối từ phía người nhận tới Mail Server.

---

## 💾 7. Bộ Nhớ Cisco & Quản Lý Sao Lưu File Cấu Hình (TFTP Server)

### 7.1. Ôn tập các loại bộ nhớ trên thiết bị Cisco

| Loại bộ nhớ | Chức năng lưu trữ | Đặc tính dữ liệu khi mất điện / Reload |
| :--- | :--- | :--- |
| **RAM** | Lưu file cấu hình hiện hành (`running-config`) | **Mất dữ liệu** (Volatile) |
| **NVRAM** | Lưu file cấu hình khởi động (`startup-config`) | **Không mất dữ liệu** (Non-volatile) |
| **Flash** | Lưu file hệ điều hành Cisco IOS | **Không mất dữ liệu** |
| **ROM** | Lưu chương trình khởi động POST & Bootstrap | Read-only |

---

### 7.2. Các câu lệnh Quản lý & Khôi phục về cấu hình gốc (Reset Device)

* **Lưu cấu hình (RAM ➔ NVRAM):**
  ```text
  Router# copy running-config startup-config
  ```
  *(hoặc lệnh ngắn gọn: `Router# write` / `Router# wr`)*

* **Xóa cấu hình cũ (Reset thiết bị trắng):**
  ```text
  Router# erase startup-config
  ```
  *(hoặc lệnh: `Router# write erase`)*

* **Khởi động lại thiết bị:**
  ```text
  Router# reload
  ```

---

### 7.3. Sao lưu & Khôi phục file cấu hình qua TFTP Server

Dịch vụ **TFTP (Trivial File Transfer Protocol)** giúp sao lưu file cấu hình (`running-config`/`startup-config`) hoặc file hệ điều hành IOS ra máy chủ trung gian để phòng ngừa hỏng hóc phần cứng.

#### A. Sao lưu file từ Switch/Router ra TFTP Server:
```text
Switch# copy running-config tftp:
Address or remote host []? 192.168.1.100    ! (Nhập địa chỉ IP của TFTP Server)
Destination filename []? Switch-Backup-Config  ! (Nhập tên file sao lưu)
```

#### B. Khôi phục file từ TFTP Server vào Switch/Router:
```text
Switch# copy tftp: running-config
Address or remote host []? 192.168.1.100    ! (Nhập địa chỉ IP của TFTP Server)
Source filename []? Switch-Backup-Config       ! (Nhập tên file sao lưu cần tải về)
Destination filename [running-config]?         ! (Nhấn Enter để giữ nguyên)
```

---

## 🔐 8. Đặt mật khẩu & Mã hóa mật khẩu

### 8.1. Mật khẩu Privileged Mode (`enable password` vs `enable secret`)

```text
Router(config)# enable password <mật_khẩu>   ! Dạng thô (Unencrypted)
Router(config)# enable secret <mật_khẩu>     ! Dạng mã hóa Hash (Khuyên dùng)
```
*(Nếu cấu hình cả 2, Cisco IOS luôn ưu tiên dùng `enable secret`)*.

---

### 8.2. Mật khẩu cổng Console (`Line Console 0`)

```text
Router(config)# line console 0
Router(config-line)# password <mật_khẩu>
Router(config-line)# login
Router(config-line)# exit
```

---

### 8.3. Mã hóa toàn bộ mật khẩu dạng thô (`service password-encryption`)

```text
Router(config)# service password-encryption
```

---

## 📢 9. Cấu hình Banner cảnh báo (`Banner MOTD`)

```text
Router(config)# banner motd #
------------------------------------------------
 CANH BAO: HE THONG QUAN TRONG!
 CHI DANG NHAP KHI DUOC PHEP.
------------------------------------------------
#
```

---

## 💡 10. Mẹo & Phím tắt hữu ích trong Cisco IOS CLI

> [!TIP]
> * **Phím `Tab`:** Tự động hoàn thành câu lệnh.
> * **Dấu `?`:** Liệt kê các lệnh có thể sử dụng tiếp theo.
> * **`Ctrl + C` hoặc `Ctrl + Shift + 6`:** Ngắt lệnh đang chạy (như `ping` hoặc ngắt tra cứu domain).
